<h1>
  <%= @journal.title %>
</h1>

<%= link_to("New Entry", new_journal_entry_path(@journal)) %>

<ul>
  <% @journal.entries.each do |entry| %>
    <li>
      <%= entry.content %>
      <% if entry.done %>
        ✅
      <% else %>
        ⛔️
      <% end %>
    </li>
  <% end %>
</ul>
